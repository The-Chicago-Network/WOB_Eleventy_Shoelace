/* import {
  create,
  search,
  insert,
} from "https://unpkg.com/@lyrasearch/lyra@latest/dist/esm/src/lyra.js";
import { tagData } from "./tagData.js";
import { filterResultsByTag } from "./filterResultsByTag.js"; */

console.log("Hello!");

/*
// Add Test Data:
const colors = create({
  schema: {
    name: "string",
    fave: "string",
    reallyfave: "string",
  },
});
const { id: saef } = insert(colors, {
  name: "redish",
  fave: "yes",
  reallyfave: "yea yesno",
});
const { id: gearg } = insert(colors, {
  name: "not red",
  fave: "yes",
  reallyfave: "ohyes yea",
});
const { id: t34g345 } = insert(colors, {
  name: "red",
  fave: "yes",
  reallyfave: "maybe",
});
const { id: gwertghrht } = insert(colors, {
  name: "maybe red",
  fave: "no",
  reallyfave: "yearp",
});
const { id: q34tgre } = insert(colors, {
  name: "green",
  fave: "yes",
  reallyfave: "string",
});

// Test Search
const searchResult = search(colors, {
  term: "red", // find "red"
  properties: ["name"], // in property "name"
});

const tags = ["yea", "yes"];

// Test filter
console.log(filterResultsByTag(searchResult.hits, tags, "reallyfave")); // return only results containing all tags
*/
